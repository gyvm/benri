package main

import (
	"go-error-demo/internal/repository"
	"go-error-demo/internal/server"
	"go-error-demo/internal/usecase"
)

func main() {
	// --- 依存性の注入 (Dependency Injection) ---
	// 各レイヤーのインスタンスを生成し、下位レイヤーのインスタンスを上位レイヤーに注入します。
	// これにより、クリーンアーキテクチャの原則に従い、各レイヤーが疎結合に保たれます。
	userRepo := repository.NewUserRepository()
	userUsecase := usecase.NewUserUsecase(userRepo)
	appServer := server.NewServer(userUsecase)

	// --- 各エラーケースのデモンストレーションを実行 ---
	// Serverの各ハンドラを順番に呼び出し、エラー処理の違いをコンソールに出力します。
	appServer.HandlePlainErrorCase()
	appServer.HandleFmtWrappedErrorCase()
	appServer.HandlePkgErrorsWrappedErrorCase()
	appServer.HandleCockroachDBErrorCase()
	appServer.HandleMultiErrorCase()
}

/*
=== Goエラーハンドリング戦略の比較解説 ===

このデモプロジェクトでは、Goにおける代表的な5つのエラーハンドリング戦略を比較しました。
それぞれのアプローチがエラー構造、スタックトレース、そしてエラー判定にどう影響するかを以下にまとめます。

--- 1. Plain Error (errors.New) ---
- 特徴: Goの最も基本的なエラー形式。`errors.New("message")` で生成される。
- スタックトレース: 保持しない。
- エラー構造: シンプルな文字列情報のみ。
- 用途: 最も単純なエラー通知で十分な場合。しかし、デバッグに必要なコンテキストが不足しがちで、
  エラーの発生源を特定するのが困難になることがあるため、プロダクションコードでの単独使用は推奨されない。

--- 2. fmt.Errorf と %w ---
- 特徴: Go 1.13で導入された標準的なエラーラッピング機能。`%w` を使うことで、元のエラーをラップし、
  エラーチェーンを形成する。
- スタックトレース: 保持しない。`%+v` で出力しても、ラップされたエラーメッセージが再帰的に表示されるだけ。
- エラー構造: エラーの連鎖構造（チェーン）を保持する。`errors.Is` や `errors.As` を使って、
  チェーン内の特定のエラーを判定できる。
- 用途: 標準ライブラリのみで完結したい場合に最適。追加のコンテキストを付与しつつ、
  元のエラー情報も保持したい場合に非常に有効。

--- 3. github.com/pkg/errors ---
- 特徴: Goコミュニティで長年デファクトスタンダードとされてきたライブラリ。エラーをラップする際に
  自動的にスタックトレースを付与する。
- スタックトレース: `Wrap` や `WithStack` を呼び出した時点でスタックトレースをキャプチャする。
  `%+v` で出力すると、メッセージ、エラーチェーン、スタックトレースが整形されて表示される。
- エラー構造: エラーチェーンを保持し、`errors.Is`/`As` とも互換性がある。
- 用途: 詳細なデバッグ情報が不可欠な場合に非常に強力。エラー発生箇所の特定が容易になるため、
  多くのプロダクション環境で採用されてきた。ただし、Go 1.13以降は標準の `%w` で十分なケースも増えている。

--- 4. github.com/cockroachdb/errors ---
- 特徴: `pkg/errors` の機能に加え、エラーの伝播、gRPC/HTTPへの変換、テレメトリーなど、
  大規模な分散システムで必要となる多くの機能を統合した高機能ライブラリ。
- スタックトレース: 自動でキャプチャし、非常に詳細な情報を提供する。
- エラー構造: 非常にリッチな構造を持つ。エラーにドメイン情報やヒントを追加できる。
- 用途: マイクロサービスや分散システムなど、複雑なエラーハンドリングが求められるプロダクション環境。
  単なるスタックトレース以上の、構造化されたエラー情報が必要な場合に適している。

--- 5. go.uber.org/multierr ---
- 特徴: 複数のエラーを単一のエラーオブジェクトに集約するためのライブラリ。
- スタックトレース: 個々のエラーがスタックトレースを持っていれば、それを保持する。
- エラー構造: `multierr.Error` という特殊な型で、内部にエラーのスライスを持つ。
  `errors.As` でこの型として取り出し、個々のエラーをループで処理できる。
  Go 1.20で導入された `errors.Join` も同様の機能を提供するが、`multierr` はより高機能。
- 用途: バッチ処理、バリデーション、複数のgoroutineの終了処理など、一度に複数のエラーが発生しうる
  シナリオで非常に有用。すべてのエラーを一度に報告できるため、処理の全体像を把握しやすくなる。
*/
